%:print 'im in latex now\n';
%: print "im still in latex now\n;"

% the output from the above prints will look odd because we are using
% single quotes in the code, so here we need to skip them or use double quotes.
% lol.

% just an empty line doesn't get the code to execute.
% btw, don't use perl! use a real language.